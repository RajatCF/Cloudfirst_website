import NavbarOptionTemplate from "./NavbarOptionTemplate";

const GoogleWorkspace = () => {
  return (
    <NavbarOptionTemplate
      title="Google Workspace"
      category="Cloud Platforms"
      summary="Modernize collaboration with Google Workspace setup, migration, security controls, and lifecycle administration."
    />
  );
};

export default GoogleWorkspace;
