import NavbarOptionTemplate from "./NavbarOptionTemplate";

const ManagedCloudOperations = () => {
  return (
    <NavbarOptionTemplate
      title="Managed cloud operations"
      category="Services"
      summary="Operate cloud workloads with proactive monitoring, lifecycle management, and continuous reliability improvements."
    />
  );
};

export default ManagedCloudOperations;
